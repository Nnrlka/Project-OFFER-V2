# Icons Directory

Place SVG icons here for custom icons.

We use React Icons library, but you can add custom SVG icons here if needed.

Example usage:
```jsx
import { ReactComponent as CustomIcon } from './icons/custom-icon.svg';